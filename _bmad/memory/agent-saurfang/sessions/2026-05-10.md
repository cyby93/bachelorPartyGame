## Session — 2026-05-10

**What happened:** Upgraded auto-targeting logic in SkillSystem.js — all three directional target finders now rank by perpendicular distance to the aim ray instead of Euclidean distance.

**Key outcomes:**
- `_findBeamTarget`, `_findRayAlly`, `_findAllyForBuff` all updated
- 60° cone filter (dot < 0.5) retained as eligibility gate — unchanged
- Ranking metric changed from `dist` to `perpDist = dist * sqrt(1 - dot²)`
- Result: within the cone, the target most aligned with your aim wins, not the nearest one
- Cyby approved the change and was satisfied

**Observations:** Cyby understood the geometry immediately and described it well before I confirmed it. Quick session — analysis, confirmation, implementation. No debate.

**Follow-up:** None open. Change is live on rebuild/v2 branch.
