## Session — Leviathan visual system

**What happened:** Diagnosed the 3-headed Leviathan animation failure (skeleton deformation can't track extra heads). Implemented Option C: directional-static sprites + engine-side pulse + aura instead of PixelLab animation.

**Key outcomes:**
- Downloaded and extracted 8 directional static rotation sprites → `public/assets/sprites/leviathan/{dir}.png`
- Added `DIRECTIONAL_STATIC_ENEMIES` set to `HostGame.js` + sprite manifest loading
- `EnemySprite.js`: directional facing (texture swap on angle change, no frame animation)
- `EnemySprite.js`: generation scaling — `spriteSize * 0.75^generation` so children visually shrink
- `EnemySprite.js`: breathing scale pulse `1 ± 5%` at 1.8 rad/s
- `EnemySprite.js`: two persistent aura rings (outer: 0x1a9e8a deep teal, inner: 0x2dd4bf bright teal) with offset pulse frequencies
- Registered leviathan in `CHARACTER_ANIMATION_PIPELINE.md` and `PIXELLAB.md`

**Observations:** Static rotations show all 3 heads correctly. The PixelLab ID is `a7aab6e3-b4d3-4d32-9fc4-b58acf24d572`. Saurfang not needed — `generation` and `angle` were already in the server DTO.

**Follow-up:** Test in-game to tune pulse intensity and aura ring sizes. Generation 0 is spriteSize 116 → canvas 124px; gens 1 and 2 will auto-scale down.
