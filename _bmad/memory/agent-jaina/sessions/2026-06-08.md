# Session 2026-06-08

## Task
Tier 3 UI work for dynamic player count scaling.

## Changes Made

### JoinScreen.svelte
- Added `GAME_CONFIG` import
- Derived `MAX_BOTS = GAME_CONFIG.MAX_PLAYERS - 1` (avoids repeating the expression)
- Added `handleBotAdd` / `handleBotRemove` functions emitting `BOT_ADD` / `BOT_REMOVE` events
- Added `.bot-section` card between player list and Start Campaign button — always visible in production
- Shows `Bots — {bots.length} / {MAX_BOTS}`, `+ Bot` (disabled at cap), `Clear All` (disabled when 0 bots)

### DebugOptions.svelte
- Added `GAME_CONFIG` import
- Changed `Bots ({botCount} / 12)` → `Bots ({botCount} / {GAME_CONFIG.MAX_PLAYERS - 1})`

### CinematicMovementSystem.js
- No change needed. Line 65 already clamps slot index: `Math.min(slotIndex, WALK_IN_FORMATION.length - 1)`. Safety already in place.

## Observations
- JoinScreen.svelte uses Svelte 5 runes (`$props`, `$derived`, `$state`) — follow that pattern, not Svelte 4 legacy mode. MEMORY.md note on "Svelte 4 legacy" is stale; real code uses runes.
- BOT_REMOVE removes all bots (not one). Labeled "Clear All" to make the behavior obvious.
