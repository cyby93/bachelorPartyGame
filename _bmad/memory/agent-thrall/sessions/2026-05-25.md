# Session 2026-05-25

## Task
Gate the cyan hitbox display line behind `GAME_CONFIG.DEBUG_HITBOXES` for three abilities: Holy Nova, Thunder Clap, and Cleave.

## Changes
**File:** `client/host/systems/OneShotEffectSystem.js`

1. Added import: `import { GAME_CONFIG } from '../../../shared/GameConfig.js'`
2. `holyNovaRing` (line ~560): wrapped cyan circle stroke in `if (GAME_CONFIG.DEBUG_HITBOXES)`
3. `thunderClapRing` (line ~719): wrapped cyan circle stroke in `if (GAME_CONFIG.DEBUG_HITBOXES)`
4. `cleaveWipe` (line ~873): wrapped cyan cone outline in `if (GAME_CONFIG.DEBUG_HITBOXES)`

`hammerStamp` (Paladin Hammer Swing) has a similar cyan line at ~line 944 — left ungated, not named in the request.

## Notes
- No sanctum content worth curating — this was a clean gating task.
