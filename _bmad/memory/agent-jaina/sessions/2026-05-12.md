# Session 2026-05-12

## Task
Resolve all four UI issues from KNOWN_ISSUES.md.

## Issues Resolved

### 1. NameScreen — required name field
- `submit()` now returns early if `name.trim()` is empty
- Button gets `disabled={!name.trim()}`
- Input gets `required` attribute
- Removed the `|| 'Player'` fallback — empty name no longer allowed through

### 2. NameScreen — design alignment with host welcome screen
- h1 color: `var(--rn-text-bright)` → `var(--rn-gold)` (matches JoinScreen)
- Card border: `var(--rn-border)` → `rgba(148, 106, 32, 0.38)` (matches JoinScreen `.card`)
- Button: `border: none; color: var(--rn-gold)` → `border: 1px solid rgba(160, 112, 30, 0.55); color: var(--rn-text-bright)` (matches HostButton primary)
- Added disabled state styling + active brightness transition

### 3. DebugOptions component
Created `client/host/components/DebugOptions.svelte`:
- Accepts `socket` prop
- Self-contained state: `debugOpen`, `selectedLevel`, `skipDialog`
- Derives `botCount` from `$gameState`
- Toggle button (🛠 Debug / 🎮 Play Mode) + collapsible debug panel
- Debug panel: level selector, skip dialog checkbox, bot add/remove

Updated `JoinScreen.svelte`:
- Replaced inline debug block with `<DebugOptions {socket} />`
- Removed: `selectedLevel`, `skipDialog`, `debugOpen`, `botCount`, `levelLabel`, `prevLevel`, `nextLevel`, `emitSetLevel`, `handleSkipDialog`, `handleBotAdd`, `handleBotRemove`
- Removed: `LEVEL_SELECT_OPTIONS` import
- Removed: all debug-related CSS (nav-btn, level-row, level-name, skip-label, bot-row, debug-section, debug-toggle)

Updated `GameHUD.svelte`:
- Added `<DebugOptions {socket} />` in actions div
- Removed: `selectedLevel`, `skipDialog`, `debugOpen` state
- Removed: `prevLevel`, `nextLevel`, `emitSetLevel`, `levelLabel` function
- Removed: `LEVEL_SELECT_OPTIONS` import
- Removed: level-row, skip-label markup (was isTraining-gated)
- Removed: nav-btn, level-row, level-name, skip-label CSS

### 4. App.svelte / GameScreen refactor
`GameScreen.svelte`:
- Removed: `comboPoints` prop, combo-pips markup, `.combo-pips`/`.pip` CSS
- Added props: `isFullscreen`, `showFullscreenBtn`, `ontogglefullscreen`, `onrejoin`
- hud-status now shows fullscreen button (if `showFullscreenBtn`) and leave button (if `onrejoin && !lobbyMode`)
- Added `.hud-btn`, `.hud-fs-btn`, `.hud-leave-btn` CSS (sized to fit 42px HUD bar)

`App.svelte`:
- Removed: `comboPoints` state, `COMBO_POINTS` socket event handler
- Removed: `fullscreen-btn` element (was App-level fixed overlay)
- Removed: `rejoin-btn` element (was sibling to GameScreen, now inside it)
- Removed: `.fullscreen-btn`, `.rejoin-btn` CSS
- Both GameScreen usages (game mode + lobby mode) now get: `showFullscreenBtn={!isPortrait && !isIOS}`, `{isFullscreen}`, `ontogglefullscreen={toggleFullscreen}`
- Game mode GameScreen also gets: `onrejoin={handleVoluntaryRejoin}`

## Files Changed
- `client/controller/screens/NameScreen.svelte`
- `client/controller/screens/GameScreen.svelte`
- `client/controller/App.svelte`
- `client/host/components/DebugOptions.svelte` (new)
- `client/host/screens/JoinScreen.svelte`
- `client/host/screens/GameHUD.svelte`
- `KNOWN_ISSUES.md` (cleared)

## Notes for MEMORY.md
Nothing new to curate — patterns used are consistent with existing component architecture.
DebugOptions follows the same socket-prop pattern as JoinScreen/GameHUD.
