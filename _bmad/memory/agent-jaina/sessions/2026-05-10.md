# Session 2026-05-10

## Work Done

Resolved 4 KNOWN_ISSUES items under "UI - Jaina":

### 1. Lobby roster matches in-game roster
- `LobbyPlayerList.svelte` — replaced the old `.player-item` layout with `.roster-row` using class icon, name in class color, and "Full" HP label
- Reuses global `.roster-row` / `.row-class-icon` / `.roster-name` / `.roster-hp` styles from `gameplay.css`

### 2. Sidebar overflow blocking quit button
- `layout.css` — `#sidebar` changed from `overflow-y: auto` to `overflow: hidden`
- `.shell-panel` gains `flex: 1; overflow-y: auto; overflow-x: hidden` so content scrolls inside the panel
- `controls.css` — `#host-actions` changed from `margin-top: auto` to `flex-shrink: 0`; quit button now always pinned at bottom

### 3. Quiz answer grid layout (controller)
- `QuizAnswerScreen.svelte` — `.options` changed from `flex-direction: column` to `grid-template-columns: 1fr 1fr`

### 4. Ability upgrade images (controller)
- `GameServer.js` — added `iconFile: skill.iconFile ?? null` to the `QUIZ_UPGRADE_OPTIONS` payload
- `UpgradeSelectScreen.svelte` — replaced emoji `<span class="icon">` with `<img class="skill-img">` (52×52px); card layout now: image left, skill-body (name + tier + changes) right

## Notes
- `iconFile` already existed in `SkillDatabase.js` for every skill; it just wasn't being sent to the client before this session
- Fallback `skill-img-fallback` span renders the emoji if `iconFile` is null (future-safe)
