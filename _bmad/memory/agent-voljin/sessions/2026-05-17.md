# Session 2026-05-17

## Task

Add file-based audio support for the controller level-up ding (fires when a player correctly answers the quiz question).

## What Was Done

- Confirmed `handleLevelUp()` was already wired to `QUIZ_RESULTS` in `App.svelte:223`.
- Added `CONTROLLER_AUDIO` export to `shared/AudioConfig.js`:
  - Key: `sfx_controller_level_up`, drop path: `/assets/sounds/sfx_controller_level_up.ogg`
- Updated `client/controller/ControllerAudio.js`:
  - Preloads the audio buffer on `init()` via `_preloadLevelUpBuffer()` (fetch + decodeAudioData, silent fail).
  - `handleLevelUp()` now plays the decoded buffer through the gain node if loaded; falls back to the synth triad if file isn't present.
  - Added `_playBuffer(buffer)` helper.

## Decisions

- File routes through the existing gain node so volume settings apply.
- Synth fallback kept intact — no file = no regression.
- Preload on `init()` (triggered by join) so buffer is ready well before quiz time.
