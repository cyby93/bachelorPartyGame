- Player roster in the lobby scene should be exactly same that is from all game levels
- sidebar content can overflow onto the quit button and in that case we can not click on quit campaign
- After each level the player has the quiz answer selection screen (controller view). Those answers should be in a grid layout (instead of 1 column like now) -> so it is more responsive 
- On the ability upgrade view (on controller), the icons should be replaced with ability images. The layout can be updated so a bigger image aligns into there

## Levels
Level 5:
- There should be more visual indication that Shade of Akama is getting buffed -> i would imagine some floating purple "+" symbols over his head. these plus signs could be more frequent for each remaining cultits enemies that are buffing him
- The cultist enemies are not properly faced. Currently, all of the facing south