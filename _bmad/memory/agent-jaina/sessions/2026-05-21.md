# Session 2026-05-21

## Task
Remove the "Enter Raid" button from the normal host UI (Training Grounds sidebar) and relocate it to the debug panel as a fallback.

## Files touched
- `client/host/screens/GameHUD.svelte` — removed `handleEnterRaid()` function and the `{#if isTraining && !isTutorialActive}` `Enter Raid` HostButton line. "Play Tutorial" remains in that same condition block.
- `client/host/components/DebugOptions.svelte` — added `handleForceEnterRaid()` that emits `EVENTS.HOST_ENTER_RAID`, added a "Campaign" section header, and a full-width "⚡ Force Enter Raid" button with gold styling. Added `.force-raid-btn` style rule.

## Files NOT touched
- `server/GameServer.js` — `_onEnterRaid` handler and socket binding unchanged. Confirmed.

## Key findings
- `DebugOptions.svelte` already existed and is already only shown in non-prod (`{#if !IS_PROD}`). The force-enter-raid button is therefore naturally gated behind that same condition — no extra guard needed.
- `EVENTS.HOST_ENTER_RAID` is defined in `shared/protocol.js` as `'host:enterRaid'`.
- The debug section already imported `EVENTS` from protocol, so no new imports were needed in DebugOptions.

## Memory update needed?
No new architectural patterns. GameHUD.svelte and DebugOptions.svelte architecture already documented in MEMORY.md.
