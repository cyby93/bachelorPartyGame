# Session 2026-05-18

## Task
Merge the objective card and level info card into one section in `GameplaySidebar.svelte`.

## What Changed
- `client/host/components/GameplaySidebar.svelte`: removed the standalone objective `sidebar-card` and extended the level info card to include the objective block (header + value + label) after the existing `gameplay-rule` divider.
- Tutorial card order unchanged: it still follows the level+objective card, now with no separate objective card after it.
- No IDs, reactive declarations, or logic touched.

## Notes
Nothing worth curating into MEMORY.md — this was a pure structural consolidation with no new patterns or architectural decisions.
