# 2026-04-28

## Session Summary

Established the full audio architecture and integration foundation for RAID NIGHT before asset gathering.

## What Landed

- Created the canonical audio specialist agent `agent-voljin` and its skill wrappers.
- Added `docs/AUDIO.md` as the audio architecture and asset integration guide.
- Added `shared/AudioConfig.js` as the shared contract for buses, level music, one-shots, player skills, boss/core-enemy mappings, and dialog voice resolution.
- Added `client/host/systems/AudioManager.js` as the host audio runtime.
- Added `client/controller/ControllerAudio.js` for minimal controller cues.
- Wired host audio to scene change, skill fire, effect damage/heal, targeted hit, boss dialog, Illidan phase transitions, aura pulse, portal beam events, player join, and player downed state transitions.
- Added host-side audio sliders for `master`, `music`, `sfx`, `voice`, plus mute.
- Added stable `levelId` to state payloads for music routing.
- Added per-level `audio.music` entries to `shared/LevelConfig.js`.
- Added placeholder `voiceKey` fields for Level 5 and 6 dialog lines.
- Normalized the dialog event from Illidan-specific naming to generic boss dialog naming.
- Added explicit mappings for all 40 player skills.
- Added explicit mappings for major boss/core-enemy sounds and encounter utility cues.

## Cyby Preferences Captured

- Host is the primary playback surface.
- Controllers stay minimal, with join/downed cues only.
- Use original WoW sounds where possible; mix in custom sounds when needed.
- One music loop per level.
- Cover player skills, boss skills, and core enemy actions.
- Design dialog VO around placeholders first.
- No controller volume UI; use phone hardware buttons.

## Open Follow-Up Work

- Gather actual music, SFX, and VO files matching the documented keys.
- Replace placeholder dialog text with final authored lines.
- Tune which keys deserve unique assets vs shared family assets once real files exist.

## Verification

- `npm run build` passed after the audio work.
- `node shared/SkillValidator.js` still has unrelated pre-existing schema issues outside the audio changes.
