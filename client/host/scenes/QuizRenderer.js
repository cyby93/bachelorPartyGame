/**
 * client/host/scenes/QuizRenderer.js
 *
 * Stub — quiz screen is now rendered by SceneOverlay.svelte (QuizScreen.svelte).
 * Quiz state is driven by quizState store (updated from socket events in main.js).
 * HostGame still needs this entry in this.renderers.
 */

export default class QuizRenderer {
  constructor(_game, _socket) {}
  setLevelMeta(_meta) {}
  enter()  {}
  exit()   {}
  update() {}
  resize() {}
}
