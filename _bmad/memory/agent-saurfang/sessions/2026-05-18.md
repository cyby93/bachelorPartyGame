## Session — isDead → isDowned rename + controller joystick fix

**What happened:** Renamed player `isDead` → `isDowned` across the full codebase and fixed the controller overlay so the joystick stays accessible while downed.

**Key outcomes:**
- Server: `ServerPlayer.js`, `GameServer.js`, `SkillSystem.js`, `StateSerializer.js`, `BotController.js` — all player `p.isDead`/`player.isDead` renamed to `isDowned`. Enemy/boss/gate entities keep `isDead`.
- DTO field name changed: server now sends `isDowned` in player state deltas (both full and delta).
- `deadPlayers` local variable renamed to `downedPlayers` in `GameServer.js`.
- Client host: `PlayerSprite.js` (`state.isDead` → `state.isDowned`, `_wasDead` → `_wasDowned`), `BaseRenderer.js`, `BattleRenderer.js`, `AudioManager.js`, `ResultScreen.svelte`, `GameplaySidebar.svelte` — all player references updated.
- `GameplaySidebar.svelte`: "Dead" text → "Downed", CSS `class:dead` → `class:downed`.
- Controller UI: `GameScreen.svelte` — wrapped skill grid in `.skill-grid-wrapper` (position: relative), moved downed overlay inside that wrapper (inset: 0 scoped to skill grid side only). Joystick (`.move-area`, left half) is now fully unobstructed. Text "You Died" → "You Are Downed", hint text updated to mention crawling.
- `App.svelte`: `isDead` → `isDowned` throughout.

**Observations:** Enemy entities (ServerEnemy, ServerBoss, ServerGate, ServerNPC, etc.) intentionally keep `isDead` — they actually die. Only players are "downed".

**Follow-up:** None.

---

## Session — Downed player crawling

**What happened:** Implemented crawl movement for downed players so they can reposition while waiting for a revive.

**Key outcomes:**
- `shared/GameConfig.js`: added `CRAWL_SPEED: 0.2` (tunable)
- `ServerPlayer.update()`: downed players now move at `CRAWL_SPEED * 60 * dt` instead of returning immediately; facing angle updates from move direction; no aiming logic
- `GameServer.js` wall collision loop: removed `p.isDead` from the skip guard so crawling players respect walls
- Ability lock already in place: `_processSkillInput()` line 1313 had `if (player.isDead) return` — no change needed
- Position deltas already serialized regardless of `isDead` — no serializer changes needed

**Observations:** Move inputs already reached dead players (no `isDead` gate in the input drain loop). The crawl path purely needed the `update()` change to consume those inputs.

**Follow-up:** Visual — Thrall needs to handle crawling animation for downed players (currently they render as static corpses; now they move).

---

## Session — Icebound Fortitude damageReduction not resetting

**What happened:** Fixed a bug where Icebound Fortitude's 60% damage reduction persisted permanently after the effect faded.

**Key outcomes:**
- Root cause: `takeDamage()` in `ServerPlayer.js` removes the Icebound Fortitude `activeEffects` entry when `shieldAbsorb` hits 0, but never calls `rebuildStats`. So `damageReduction` stays at 0.6. Later when the timer fires, `activeEffects.length` is unchanged — `rebuildStats` is skipped again. Reduction is stuck forever.
- Fix: added `rebuildStats(this)` immediately after the `activeEffects.filter` inside the `shieldAbsorb === 0` branch. One line, `ServerPlayer.js:138`.
- Side effect is correct: overflow damage from the hit that depleted the shield is no longer reduced — effect is fully gone at depletion.

**Observations:** `rebuildStats` was already imported in `ServerPlayer.js`. The expiry path (timer fires) was already correct; the only broken path was shield-depletion-before-expiry.

**Follow-up:** None. Clean fix, no open questions.

---

## Session — Rogue rectangular melee hitbox

**What happened:** Replaced Sinister Strike's cone hitbox with an oriented rectangle for more precise, longer-reach melee.

**Key outcomes:**
- `CollisionSystem.js`: added `inOrientedRect(origin, dirVector, range, halfWidth, target)` — projects target onto forward axis and lateral axis, checks both within bounds
- `SkillSystem.js` `_executeMelee()`: detects `config.width != null`; if set, uses `inOrientedRect` instead of `inCone`. Local `_inHitbox()` closure hides the branch from all four hit loops (enemies, boss, gates, buildings)
- `SkillDatabase.js` Sinister Strike: removed `angle`, added `range: 160, width: 80`. Other MELEE skills (Warrior Cleave, Paladin Hammer Swing, DK Obliterate) unchanged — still use `angle`/`inCone`
- `GameServer.js`: SKILL_FIRED payload now includes `width: config.width` when present — Thrall needs this for VFX

**Observations:** The `width` config property is the discriminator — no new `type`/`subtype` needed. Existing melee classes unaffected (they don't have `width`). Config values (range 160, width 80) are starting points for tuning.

**Follow-up (Thrall):** Completed same session — `VFXManager.js` MELEE handler branches on `d.width`; `OneShotEffectSystem.meleeRect()` added.

---

## Session — Sinister Strike rect hitbox edge-miss bug

**What happened:** Melee rect was doing a point-in-rect test against enemy center; hits registered only when center was inside the rectangle, not when the enemy body (oval) overlapped the rect edge.

**Key outcomes:**
- `CollisionSystem.inOrientedRect()`: added optional `targetRadius = 0` param. When > 0, uses closest-point expansion: clamps `(forward, lateral)` to rect bounds, computes dist² from target center to that closest point, returns true if `dist² <= targetRadius²`. This is a rect-vs-circle overlap check in the rect's local frame.
- `SkillSystem._executeMelee()`: `_inHitbox(tx, ty, tr = 0)` now accepts a radius; all four hit loops pass the target's `.radius` (enemies, boss, gates, buildings). Cone path ignores the radius (unchanged — point check only, no reported issues there).

**Observations:** Training dummy radius = 30 (from EnemyTypeConfig). Enemy oval stores `radiusX = radius/2`, `radiusY = radius` — but the collision expansion uses `.radius` (the larger axis), which is intentionally generous and consistent with other server collision checks.
