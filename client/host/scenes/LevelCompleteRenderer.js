/**
 * client/host/scenes/LevelCompleteRenderer.js
 *
 * Stub — level-complete screen is now rendered by SceneOverlay.svelte
 * (LevelCompleteScreen.svelte). HostGame still needs this entry in
 * this.renderers, but the canvas does nothing.
 */

export default class LevelCompleteRenderer {
  constructor(_game, _socket) {}
  setLevelMeta(_meta) {}
  enter()  {}
  exit()   {}
  update() {}
  resize() {}
}
